# Panchanga + Auth API base URL (no trailing slash).
# The same backend serves panchanga data AND the auth/profile routes.
# Local:  http://localhost:8080  (see nepali-holiday-api/README.md)
# Remote: https://193-123-67-133.sslip.io
#
# Tip: for local-only overrides create a git-ignored .env.local with this same
# key — it takes precedence over .env without editing the committed file.
VITE_API_BASE_URL=http://localhost:8080
